<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mli.lookgo.module.auth.dao.AuthDao">

    <insert id="createUser" parameterType="com.mli.lookgo.module.user.model.entity.User"
            useGeneratedKeys="true" keyProperty="user.id" keyColumn="id">
        INSERT INTO [dbo].[users]
            (membership_tier_id, role_id, email, password, username, birth_date, status, created_at, updated_at, last_login_at)
        VALUES
            (#{user.membershipTierId}, #{user.roleId}, #{user.email}, #{user.password},
             #{user.username}, #{user.birthDate, jdbcType=DATE}, #{user.status}, #{user.createdAt}, #{user.updatedAt}, #{user.lastLoginAt, jdbcType=TIMESTAMP})
    </insert>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(1)
        FROM [dbo].[users]
        WHERE email = #{email}
    </select>

    <update id="updatePasswordByEmail">
        UPDATE [dbo].[users]
        SET password = #{newPassword}, updated_at = #{updatedAt}
        WHERE email = #{email}
    </update>

</mapper>
